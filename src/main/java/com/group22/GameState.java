package com.group22;

/**
 * 
 * The enum {@code GameState} represents the game state of {@code Engine}.
 * 
 * @author Liam Clegg
 * @version 1.0
 */
public enum GameState {
    ReplaysBrowser, ScoresBrowser, LevelSelector, ProfileSelector, Start, Playing, Paused, GameOver, LevelComplete, Loading, CREDITS
}

